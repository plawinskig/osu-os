# extlinux configuration - read by the syslinux bootloader stage
# installed in the MBR/boot sector. Selects the kernel and passes
# the initial kernel command line.
DEFAULT osukiosk
TIMEOUT 10
PROMPT 0

LABEL osukiosk
    KERNEL /bzImage
    APPEND console=tty1 quiet
